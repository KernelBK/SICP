
; T 2.25 使用car cdr get 7
(define list-1 '(1 3 (5 7) 9))
(define list-2 '((7)))
(define list-3 '(1 (2 (3 (4 (5 (6 7)))))))

; for1 (car (cdr (car (cdr (cdr list-1)))))
; for2 (car (car list-2))
; for3 (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr list-3))))))))))))

;   (car (cdr (car (cdr (cdr list-1)))))
;=> 7

;   (car (car list-2))
;=> 7

;   (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr list-3))))))))))))
;=> 7
