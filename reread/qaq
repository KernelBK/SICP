
lisp 前缀语法
想起《c算法实现》中 递归求前缀表达式的值
另外 树形积累 怎么求前缀表达式

***抽象
define 用名字去引用一个组合计算的结果

***环境
名字-值  对偶的轨迹
环境所扮演的角色就是用于确定表达式中各个符号的意义。

***基本元素
数和算术运算是基本数据和过程；
组合嵌套提供了组织起多个操作的方法；
定义是一种受限的抽象手段，它为名字关联相应的值。

***过程定义，一种更强大的抽象技术，通过它可以为复合操作提供名字。
(define (square x) (* x x))

过程 ——> 复合过程
匿名过程

正则序求值——完全展开而后归约
应用序求值——先求值参数而后应用（现在解释器里实际使用的）

***条件表达式和谓词
(define (abs a)
  (cond
    ((< a 0) (- a))
    (else a)
    )
  )
  
***复合
and or not
(define (>= x y)
  (not (< x y))
  )
  


